services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: geo_workbench
      POSTGRES_USER: geo
      POSTGRES_PASSWORD: geo_password
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
